const assert = require('assert');
const db = require('../src/db');

describe('db', () => {
    describe('test', () => {
        it('1=1', () => {
            assert.equal(1, 1);
        });
    });
});
